# Served by config-server to the client named "eureka-server" (spring.application.name).
management:
  tracing:
    sampling:
      # Trace everything - this is a learning/demo project. Lower this for a real deployment.
      probability: 1.0
